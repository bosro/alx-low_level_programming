#include "main.h"
#include <stdio.h>
#include <math.h>

/**
 * main - main function that implements max_PrimeNum
 *
 * Return: returns 0
 */

int main(long maxnum)
{
	long n;
	n = 612852475143;
	printf("%li\n", max_PrimeNum(n));
	return (maxnum);
}
