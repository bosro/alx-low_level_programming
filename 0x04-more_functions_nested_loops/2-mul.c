#include <stdio.h>
#include <math.h>

/**
 * mul - multiplies two integers
 * @a: first function parameter
 * @b: second parameter
 * Return: returns multplied results
 */

int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
